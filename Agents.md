# Agents.md — 工具转化工程总规范

## 项目定位
本目录下的**所有工程都是工具转化工程**：将现有工具（如 markitdown 等）改造成**普通人在手机上直接可用**的版本，用户无需任何技术背景。这是所有改造工作的最终目的，任何取舍都以它为准。

## 改造规则（必须遵守）

### 1. 手机适配 + 跨平台
- 所有工具都必须做成**手机适配版本**。
- 采用 **Tauri** 实现跨平台（Android / iOS / 桌面）。

### 2. 代码存放位置
- 所有改造后的代码统一放在对应工程目录下的 **`Tauri` 总文件夹**中。
- 示例：`markitdown-main/Tauri/`。
- 原始工具代码保持不动，改造代码不与原始代码混放。

### 3. 界面要求
- 封装的工具必须有**优美、直观的界面**。
- 界面布局要**依据该工具自身的特点来设计**，不做千篇一律的套壳。
- 面向不懂技术的用户：操作路径短、文案直白、开箱即用。

### 4. 语言改造规则
- **手机跑不了的语言统一改写为 Rust 版本**（典型如 Python）。
- 如果某些依赖包 Rust 不支持，则**退而改造成 C++ 版本**。
- 改造后功能必须与原工具对齐，不允许静默丢失功能。

## 交付标准
完成的每个工具版本应满足：
1. 手机上可直接安装运行（Tauri 打包）。
2. 界面美观且贴合该工具的使用特点。
3. 功能与原工具一致，无技术用户也能独立使用。
